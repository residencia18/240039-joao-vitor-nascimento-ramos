import { NovinhaDirective } from './novinha.directive';

describe('NovinhaDirective', () => {
  it('should create an instance', () => {
    const directive = new NovinhaDirective();
    expect(directive).toBeTruthy();
  });
});
