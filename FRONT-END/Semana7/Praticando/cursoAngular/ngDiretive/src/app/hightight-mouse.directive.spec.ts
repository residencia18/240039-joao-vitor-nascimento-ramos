import { HightightMouseDirective } from './hightight-mouse.directive';

describe('HightightMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HightightMouseDirective();
    expect(directive).toBeTruthy();
  });
});
