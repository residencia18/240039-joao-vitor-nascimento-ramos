#ifndef VERIFICACOES_H
#define VERIFICACOES_H

#include "estruturas.h"

bool verificaPreco(float );
bool nomePadrao(string );
bool codigoPadrao(string );

bool codigoDisponivel(vector<Produto> & , string );
bool nomeDisponivel(vector<Produto> &, string );

#endif  